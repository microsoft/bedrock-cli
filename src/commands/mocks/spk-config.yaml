# Azure DevOps Configuration
azure_devops:
  access_token: "hpe3a9oiswgcodtfdpzfiek3saxbrh5if1fp673xihgc5ap467a" # This is your Personal Access Token with permission to modify and access this private repo. Leave this empty if project is public
  hld_repository: "https://dev.azure.com/bhnook/fabrikam/_git/hld" # Repository URL for your Bedrock HLDs
  manifest_repository: "https://dev.azure.com/bhnook/fabrikam/_git/materialized" # Repository URL that is configured for flux. This holds the kubernetes manifests that is generated by fabrikate.
  infra_repository: "https://dev.azure.com/naros32/bedrock_testing_phase2/_git/spk-testing-private-azdo" # Repository URL that contains your terraform templates to be sed for scaffolding and generating infrastructure deployment templates.
  org: "bhnook" # Your AzDo Org
  project: "fabrikam" # Your AzDo project
infra:
  # DO NOT MODIFY THE FOLLOWING
  # TO-DO: Prerequisite versions are auto-generated upon executing `spk init`
  terraform: "v0.12.9"
  helm: "v2.14.3"
  git: "2.17.1"
  az: "2.0.71"

introspection:
  dashboard:
    image: "samiyaakhtar/spektate:prod"
    name: "spektate"
  azure: # This is the storage account for the service introspection tool
    account_name: "${env:test_name}"
    table_name: "${env:test_name}+${env:test_key}"
    partition_key: "partition-key"
    key: "${env:test_key}"
    # Following is needed if using spk to onboard to use introspection tool
    service_principal_id: "service-principal-id"
    service_principal_secret: "service-principal-secret"
    subscription_id: "subscription-id"
    tenant_id: "tenant-id"
key_vault_name:
