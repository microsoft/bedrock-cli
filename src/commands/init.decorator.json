{
  "command": "init",
  "alias": "i",
  "description": "Initialize the bedrock tool for the first time.",
  "options": [
    {
      "arg": "-f, --file <config-file-path>",
      "description": "Path to the config file."
    },
    {
      "arg": "-i, --interactive",
      "description": "Interactive mode. Creates a config file (config.yaml) in a folder .bedrock under your home directory",
      "defaultValue": false
    }
  ]
}
